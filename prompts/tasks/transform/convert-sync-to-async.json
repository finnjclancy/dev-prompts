{
  "title": "convert sync code to async",
  "description": "transform blocking code to async with proper error handling and backpressure.",
  "language": "any",
  "tech": null,
  "category": "tasks",
  "subcategory": "transform",
  "tags": ["async", "refactor", "performance"],
  "inputs": [
    { "name": "code_selection", "type": "code", "required": true }
  ],
  "context": "preserve behavior; avoid unbounded concurrency; handle cancellation.",
  "instructions": "1) identify blocking calls and replace with async equivalents. 2) add concurrency limits. 3) propagate cancellation and timeouts. 4) show before/after code. return code only.",
  "model_tips": "avoid fire-and-forget.",
  "risks": "deadlocks; race conditions.",
  "references": [],
  "output_format": { "type": "code" }
}
